# Agent Rules

Obey `self` tool description every response, every turn. That description holds full rules.

When entering an unfamiliar project or directory, use the `tree` tool first with `maxDepth` 2–3 to map its structure. Use `rg` or `grep` for content searches.

`self` execute does nothing. Rules apply without calling. Never call `self` to satisfy rules.
