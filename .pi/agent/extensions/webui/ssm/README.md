# Session manager (`/ssm`)

Detached catalog on **http://127.0.0.1:17300/ssm**. Not tied to a pi session pid.

See parent `webui/README.md` for daemon lifecycle.

**Open** = read-only `/view`. Archive: `~/.pi/agent/ssm-archive.json`.
