Title: Lovable App
URL: https://project-dynamo.learn.joinhandshake.com/submit/checklist

Final checklistFirst clear the build checklist — task structure, instruction, Dockerfile, tests, and a genuine solution all live there. This page is the short list of validation-result gates to confirm right before you push:Work through each item.Checklist0 of 4 completeOracle scores reward 1.0 (harbor run -p . --agent oracle); nop scores reward < 1.0 (harbor run -p . --agent nop).The [agent].timeout_sec in task.toml matches the "You have N seconds…" line in instruction.md, and is justified by the oracle's actual runtime (with headroom) — not padded or guessed.Pass@2 cleared the 3600s cap without timing out (see /submit/pass-at).Pass@5 lands in the 0–2/5 band — recorded in the PR. 0/5 (all valid failures, oracle passes, no timeouts) is the hardest case and is good. 3–5/5 means too easy or the verifier is too loose — do NOT submit. A 0/5 from timeouts or ambiguity is broken, not hard — do NOT submit, needs fixing.PreviousNext
